# Pinnuté verze — snapshot úspěšné instalace 2026-04-22.
# Postup aktualizace: instructions-reference.md sekce 7 "Jak pinout verze".

# --- GUI ---
PySide6==6.7.3

# --- DB ---
SQLAlchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.3
python-dotenv==1.2.2

# --- Spot SDK (4.0.x → 5.1.4 po update) ---
bosdyn-client==5.1.4
bosdyn-core==5.1.4
bosdyn-api==5.1.4
bosdyn-mission==5.1.4

# --- Image processing ---
opencv-python==4.11.0.86
numpy==1.26.4
Pillow==12.2.0

# --- OCR pipeline ---
ultralytics==8.4.41
onnxruntime==1.23.2
fast-plate-ocr==1.1.0

# --- OCR fallback engine (izolován do subprocessu, ale musí být v hlavním venv
# aby ho import v subprocess wrapperu našel; technický dluh viz instructions.md
# "POC known limitations" sekce "Nice to have") ---
nomeroff_net==4.0.1

# --- Credentials ---
keyring==25.7.0

# --- Bundler (dříve v setup_venv.bat; sjednocený source of truth) ---
pyinstaller==6.19.0

# --- Dev / testy ---
pytest==9.0.3
pytest-postgresql==8.0.0
pytest-qt==4.5.0
